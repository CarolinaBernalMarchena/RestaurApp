import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '2024-training-3er-quarter',
  webDir: 'www'
};

export default config;
